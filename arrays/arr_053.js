let masNumeros = [ 43, 11, 18, 46, 44, 37, 42, 29, 9, 3, 37, 0, 40, 10, 38, 34, 25, 40, 4, 32 ];

let impares = masNumeros.reduce(function(acc, unidad){
    if (unidad % 2 !== 0){
        acc.push(unidad);
    }
    return impar;
}, impar = [])

let numerosImpares = impar;
console.log("impares: ", numerosImpares);